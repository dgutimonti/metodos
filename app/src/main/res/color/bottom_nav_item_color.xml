<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- El color cuando el ítem está seleccionado (usamos el color de acento) -->
    <item android:color="@color/colorAccent" android:state_checked="true" />

    <!-- El color por defecto para los ítems no seleccionados (un gris secundario) -->
    <item android:color="@color/text_secondary_light" android:state_checked="false"/>
</selector>
